# Configuração para o Next.js na porta principal (80)
#server {
#    listen 80;
#    server_name _;  # Qualquer nome de servidor (padrão)
    
#    location / {
#        proxy_pass http://nextjs:3000;  # Encaminha para o serviço Next.js
#        proxy_http_version 1.1;
#        proxy_set_header Upgrade $http_upgrade;
#
#
#
#
#        proxy_set_header Connection 'upgrade';
#       proxy_set_header Host $host;
#        proxy_cache_bypass $http_upgrade;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }

 #   location /.well-known/acme-challenge/ {
 #       root /var/www/certbot;
 #   }
 #}

# Configuração para o Laravel Reverb WebSockets
server {
    listen 8081;
    server_name _;

    location / {
        proxy_pass http://reverb:8081;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}

# Configuração para o Laravel/PHP na porta 8080
server {
    listen 8080;
    server_name _;  # Qualquer nome de servidor
    root /var/www/html/public;
    index index.php index.html;
    client_max_body_size 5M;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}